apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "agent-platform-standalone.musterFullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-platform-standalone.labels.common" . | nindent 4 }}
    {{- with (include "agent-platform-standalone.httpRouteLabels" (dict "ctx" . "route" "muster")) }}
    {{- . | nindent 4 }}
    {{- end }}
  {{- with (include "agent-platform-standalone.httpRouteAnnotations" (dict "ctx" . "route" "muster")) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  parentRefs:
    {{- toYaml .Values.ingress.parentRefs | nindent 4 }}
  {{- with .Values.ingress.hostnames }}
  hostnames:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "agent-platform-standalone.musterFullname" . }}
          port: {{ include "agent-platform-standalone.musterServicePort" . }}
