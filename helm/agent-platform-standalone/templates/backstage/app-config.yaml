{{- if .Values.components.backstage.enabled }}
{{- $backstage := .Values.components.backstage }}
{{- /* The domain is required whenever Backstage is on: baseDomain and the
avatars CSP entry derive from it even when components.backstage.hostname
overrides the route hostname. */}}
{{- $domain := required "global.domain is empty but the Backstage component is on: the app-config's baseDomain and avatars CSP entry derive from it" .Values.global.domain }}
{{- $hostname := include "agent-platform-standalone.hostname" (dict "ctx" . "prefix" "backstage" "override" $backstage.hostname "key" "components.backstage.hostname") }}
{{- $issuer := include "agent-platform-standalone.issuerUrl" (dict "ctx" . "for" "the Backstage login provider") }}
{{- $provider := "oidc-agent-platform" }}
{{- $musterHostname := "" }}
{{- if .Values.ingress.hostnames }}
{{- $musterHostname = index .Values.ingress.hostnames 0 }}
{{- else }}
{{- $musterHostname = include "agent-platform-standalone.hostname" (dict "ctx" . "prefix" "muster" "key" "ingress.hostnames") }}
{{- end }}
# Backstage app-config layered on the image's own: the login provider from
# global.identity, one installation named after the release, the matching
# in-cluster Kubernetes entry (the frontend mints the kagent token from it),
# the muster installation, and app.extensions limited to the Agent Platform
# pages. The backstage chart mounts it through backstage.extraAppConfig; the
# ConfigMap name and the file name are fixed there.
apiVersion: v1
kind: ConfigMap
metadata:
  name: agent-platform-backstage-app-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "agent-platform-standalone.labels.common" . | nindent 4 }}
data:
  app-config.agent-platform.yaml: |
    app:
      title: Agent Platform
      baseUrl: https://{{ $hostname }}
      rootRedirect: /agent-platform
      extensions:
        - page:agent-platform
        - nav-item:agent-platform
        {{- range $backstage.enabledExtensions }}
        - {{ . }}
        {{- end }}
        {{- /* enabledExtensions wins: an extension listed in both is enabled. */}}
        {{- range $backstage.disabledExtensions }}
        {{- if not (has . $backstage.enabledExtensions) }}
        - {{ . }}: false
        {{- end }}
        {{- end }}
    backend:
      baseUrl: https://{{ $hostname }}
      cors:
        origin: https://{{ $hostname }}
      csp:
        img-src:
          - "'self'"
          - "data:"
          - https://avatars.{{ $domain }}
    auth:
      environment: production
      session:
        secret: ${AUTH_SESSION_SECRET}
      providers:
        {{ $provider }}:
          production:
            metadataUrl: {{ $issuer }}/.well-known/openid-configuration
            clientId: {{ .Values.global.identity.clientId }}
            clientSecret: ${AGENT_PLATFORM_OIDC_CLIENT_SECRET}
            {{- /* Unset, the Backstage oidc authenticator sends prompt=none
            on every authorize request. Dex ignores it, but a spec-strict
            IdP (Google) answers a fresh login with error=interaction_required
            — surfaced misleadingly as "iss missing from the response",
            because the error redirect carries no RFC 9207 iss parameter and
            openid-client checks that first. auto lets the IdP decide. */}}
            prompt: auto
    gs:
      authProvider: {{ $provider }}
      {{- with $backstage.extraScopes }}
      auth:
        extraScopes:
          {{- toYaml . | nindent 10 }}
      {{- end }}
      installations:
        {{ .Release.Name }}:
          authProvider: oidc
          oidcTokenProvider: {{ $provider }}
          pipeline: stable
          baseDomain: {{ $domain }}
          {{- /* Standalone installations have no Giant Swarm observability
          stack at observability.<baseDomain>. Backstage >= 0.200.26 reads
          this and renders neutral "metrics unavailable" states instead of
          querying Mimir and erroring; older versions ignore the key. */}}
          mimirEnabled: false
    kubernetes:
      serviceLocatorMethod:
        type: multiTenant
      clusterLocatorMethods:
        - type: config
          clusters:
            - name: {{ .Release.Name }}
              url: https://kubernetes.default.svc
              caFile: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
              authProvider: oidc
              oidcTokenProvider: {{ $provider }}
    muster:
      installations:
        - name: {{ .Release.Name }}
          {{- /* The full MCP endpoint, not the bare hostname: the muster
          backend plugin POSTs MCP Streamable-HTTP to this URL verbatim, and
          muster serves MCP at /mcp (the bare origin answers 404). */}}
          url: https://{{ $musterHostname }}/mcp
          authProvider: {{ $provider }}
    {{- $kagentRoute := dict }}
    {{- if and (include "agent-platform-standalone.componentEnabled" (dict "root" . "name" "kagent")) .Values.components.kagent.controllerRoute.enabled }}
    {{- $kagentRoute = .Values.components.kagent.controllerRoute }}
    {{- end }}
    {{- if or $backstage.skillsRepositories $kagentRoute }}
    agentPlatform:
      {{- with $backstage.skillsRepositories }}
      skills:
        repositories:
          {{- toYaml . | nindent 10 }}
      {{- end }}
      {{- if $kagentRoute }}
      {{- /* Without this the plugin derives kagent.<baseDomain>/api — the
      kagent UI hostname, which this umbrella fronts with oauth2-proxy at
      most, never with the controller API. The controller API lives on the
      controllerRoute: <hostname><pathPrefix>/* with the prefix stripped
      before kagent, so its /api sits at <hostname><pathPrefix>/api. */}}
      kagent:
        installations:
          {{ .Release.Name }}:
            apiBaseUrl: https://{{ include "agent-platform-standalone.hostname" (dict "ctx" . "prefix" "agentgateway" "override" $kagentRoute.hostname "key" "components.kagent.controllerRoute.hostname") }}{{ $kagentRoute.pathPrefix }}/api
      {{- end }}
    {{- end }}
{{- end }}
