vendor
.vendir*

# Helm pulls dependency archives here; Chart.lock is the committed BOM.
helm/*/charts/
*.curate
build/

# Python bytecode (ATS test runs)
__pycache__/

# uv virtualenv of the ATS test suite (created by `uv sync` in tests/ats)
tests/ats/.venv/
